import * as sourceContent from '../data/siteContent.js';

// The existing English portfolio remains the single source of truth.
// English strings are not duplicated here; they are consumed directly.
export const en = {
  code: 'en',
  label: 'EN',
  content: sourceContent,
  translations: {},
};
